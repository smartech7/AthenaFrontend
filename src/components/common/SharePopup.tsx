import * as React from 'react';

interface ISharePopupProps {
  link: string;
}

const SharePopup: React.FunctionComponent<ISharePopupProps> = ({link}) => {
  return <div className='rounded-xl '></div>
};

export default SharePopup;
